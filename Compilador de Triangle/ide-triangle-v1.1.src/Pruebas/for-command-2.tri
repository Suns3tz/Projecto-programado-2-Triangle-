let
  type fecha ~ record
    d : Integer,
    m : Integer
  end;
  var hoy: fecha;
  var c : Char;
  func inicio(n : Integer) : Integer ~ n + 1
in 
  for hoy.d := 30 downto inicio(3) do begin
    get(var c);
    put(c)
  end